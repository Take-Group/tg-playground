[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tg-playground-backend"
version = "0.1.0"
description = "TG Playground Backend"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139",
    "uvicorn[standard]>=0.51",
    "pydantic>=2.13",
    "pydantic-settings>=2.14.2",
    "sqlalchemy[asyncio]>=2.0.51",
    "asyncpg>=0.31",
    "alembic>=1.18",
    "redis>=8.0",
    "temporalio>=1.30",
    "structlog>=26.1",
    "httpx>=0.28",
]

[project.scripts]
serve = "app.main:run"
worker = "app.temporal.worker:run"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "SIM", "TCH"]

[tool.ruff.lint.flake8-type-checking]
# FastAPI ewaluuje adnotacje w runtime — importy typów z collections.abc muszą zostać poza TYPE_CHECKING
exempt-modules = ["typing", "collections.abc"]

[tool.ruff.lint.isort]
known-first-party = ["app"]
